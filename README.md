# Banco de Prompts do TRF2

Atenção, depois de clonar esse repositório, ative a validação de prompts no "pre-commit" executando a seguinte linha de comando no terminal:

```bash
git config core.hooksPath .githooks
```